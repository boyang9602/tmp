{
    "org.apache.commons.math.MathRuntimeException": [
        {
            "createIllegalArgumentException(java.lang.String, java.lang.Object[])": [
                {
                    "ENTER": [
                        {
                            "Patch55.inv.output": {
                                "invs": [
                                    "pattern.toString one of { \"endpoints do not specify an interval: [{0}, {1}]\", \"function values at endpoints do not have different signs.  Endpoints: [{0}, {1}], Values: [{2}, {3}]\", \"invalid interval, initial value parameters:  lower={0}, initial={1}, upper={2}\" }",
                                    "size(arguments[]) one of { 2, 3, 4 }"
                                ]
                            }
                        },
                        {
                            "b.inv.output": {
                                "invs": [
                                    "pattern.toString one of { \"endpoints do not specify an interval: [{0}, {1}]\", \"function values at endpoints do not have different signs.  Endpoints: [{0}, {1}], Values: [{2}, {3}]\" }",
                                    "size(arguments[]) one of { 2, 4 }"
                                ]
                            }
                        },
                        {
                            "Patch170.inv.output": {
                                "invs": [
                                    "pattern.toString one of { \"endpoints do not specify an interval: [{0}, {1}]\", \"function values at endpoints do not have different signs.  Endpoints: [{0}, {1}], Values: [{2}, {3}]\", \"invalid interval, initial value parameters:  lower={0}, initial={1}, upper={2}\" }",
                                    "size(arguments[]) one of { 2, 3, 4 }"
                                ]
                            }
                        }
                    ]
                },
                {
                    "EXIT": [
                        {
                            "Patch55.inv.output": {
                                "invs": [
                                    "pattern.toString one of { \"endpoints do not specify an interval: [{0}, {1}]\", \"function values at endpoints do not have different signs.  Endpoints: [{0}, {1}], Values: [{2}, {3}]\", \"invalid interval, initial value parameters:  lower={0}, initial={1}, upper={2}\" }"
                                ]
                            }
                        },
                        {
                            "b.inv.output": {
                                "invs": [
                                    "pattern.toString one of { \"endpoints do not specify an interval: [{0}, {1}]\", \"function values at endpoints do not have different signs.  Endpoints: [{0}, {1}], Values: [{2}, {3}]\" }"
                                ]
                            }
                        },
                        {
                            "Patch170.inv.output": {
                                "invs": [
                                    "pattern.toString one of { \"endpoints do not specify an interval: [{0}, {1}]\", \"function values at endpoints do not have different signs.  Endpoints: [{0}, {1}], Values: [{2}, {3}]\", \"invalid interval, initial value parameters:  lower={0}, initial={1}, upper={2}\" }"
                                ]
                            }
                        }
                    ]
                }
            ]
        }
    ]
}